export enum FuckingExerciseUnits {
  DURATION_SECONDS = 'duration-seconds',
  DURATION_MINUTES = 'duration-minutes',
  QUANTITY = 'quantity'
}

export enum FuckingExerciseSickness {
  PLEB= 'pleb',
  GYM_BRO = 'gym-bro',
  EDDIE_HALL = 'eddie-hall',
}

export enum FuckingExerciseType {
  PRESSUP = 'pressup',
  SITUP = 'situp',
  STAR_JUMPS = 'star-jumps',
}
